# Optional Airflow local quickstart note

For a fast resume project, you do not need to fully deploy Airflow.
But if you want to run the DAG, use Airflow's official Docker Compose setup, then mount this project inside /opt/airflow/.

Airflow docs:
https://airflow.apache.org/docs/apache-airflow/stable/howto/docker-compose/index.html
